
{
  "entities": {
    "GlobalSettings": {
      "title": "Global Settings",
      "description": "Global application configuration including site password and session management.",
      "type": "object",
      "properties": {
        "websitePassword": {
          "type": "string",
          "description": "The global password required to access the site."
        },
        "forceLogoutVersion": {
          "type": "number",
          "description": "Counter used to invalidate all active sessions."
        }
      },
      "required": ["websitePassword"]
    },
    "PaymentRequest": {
      "title": "Payment Request",
      "description": "A request for search access triggered by a payment.",
      "type": "object",
      "properties": {
        "requestId": { "type": "string" },
        "sessionId": { "type": "string" },
        "status": { 
          "type": "string", 
          "enum": ["WAITING_APPROVAL", "APPROVED", "DECLINED", "USED"] 
        },
        "timestamp": { "type": "number" },
        "amount": { "type": "number" }
      },
      "required": ["requestId", "sessionId", "status", "timestamp"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/settings/global": {
      "schema": "GlobalSettings",
      "description": "Singleton document for global application settings."
    },
    "/payment_requests/{requestId}": {
      "schema": "PaymentRequest",
      "description": "Individual payment verification requests."
    }
  }
}
